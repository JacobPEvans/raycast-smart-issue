name: CI
on:
  pull_request:
    branches: [main]
jobs:
  build-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - run: bun install --frozen-lockfile
      - name: Generate package-lock.json for ray lint validation
        run: npm install --package-lock-only --ignore-scripts
      - run: bun run build
      - run: bun run lint
