name: Markdown Lint
on:
  pull_request:
    paths: ['**/*.md']
  push:
    branches: [main]
    paths: ['**/*.md']
jobs:
  lint:
    uses: JacobPEvans/.github/.github/workflows/_markdown-lint.yml@main
